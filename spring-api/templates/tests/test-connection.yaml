apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-api.fullname" . }}-test-connection"
  labels:
    {{- include "spring-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
